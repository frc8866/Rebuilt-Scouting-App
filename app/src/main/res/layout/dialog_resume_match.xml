<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You have an unfinished scouting session."
        android:textColor="@color/text_dim"
        android:textSize="14sp"
        android:layout_marginBottom="12dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/resume_highlight_bg"
        android:padding="14dp">

        <include layout="@layout/row_info" android:id="@+id/row_match" />
        <include layout="@layout/row_info" android:id="@+id/row_team" />
        <include layout="@layout/row_info" android:id="@+id/row_position" />
        <include layout="@layout/row_info" android:id="@+id/row_scouter" />
        <include layout="@layout/row_info" android:id="@+id/row_time_left" />
    </LinearLayout>
</LinearLayout>
